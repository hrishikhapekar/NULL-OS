# Copy this to .env.local for local dev (not needed — vite proxy handles it)
# Set this in Vercel dashboard under Project > Settings > Environment Variables
VITE_API_URL=https://YOUR-HF-USERNAME-null-os-api.hf.space
